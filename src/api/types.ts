export interface BloodResponse {
  tipo: string;
  nivel_atual: number;
  prioritario: boolean;
  ultima_atualizacao: string;
}

export interface Blood {
  type: string;
  level: number;
  priority: boolean;
  updatedAt: string;
}
